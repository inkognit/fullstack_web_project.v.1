import { Container, Grid } from "@material-ui/core";
import { FC } from "react";

export const User: FC<{}> = () => {
    return (
        <Container>
            <Grid>
                User
            </Grid>
        </Container>
    )
}